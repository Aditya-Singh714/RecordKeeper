<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit User</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
  <div class="min-h-screen flex items-center justify-center bg-zinc-800 p-6">
    <div class="w-full max-w-2xl bg-zinc-700 p-8 rounded-2xl shadow-2xl">
      <h1 class="text-3xl font-bold text-white tracking-tight mb-6 text-center">Update User</h1>
      <form action="/update/<%= user._id %>" method="post" class="space-y-4">
        <div class="flex flex-col md:flex-row md:space-x-4">
          <input type="text" name="name" placeholder="Enter your name" value="<%= user.name %>"
            class="flex-1 px-4 py-3 border border-zinc-600 bg-transparent text-white placeholder-zinc-400 rounded-lg focus:outline-none focus:ring-2 focus:ring-teal-400 transition" />
        </div>
        <div class="flex flex-col md:flex-row md:space-x-4">
          <input type="text" name="email" placeholder="Enter your email" value="<%= user.email %>"
            class="flex-1 px-4 py-3 border border-zinc-600 bg-transparent text-white placeholder-zinc-400 rounded-lg focus:outline-none focus:ring-2 focus:ring-teal-400 transition" />
        </div>
        <div class="flex flex-col md:flex-row md:space-x-4">
          <input type="text" name="image" placeholder="Enter your image URL" value="<%= user.image %>"
            class="flex-1 px-4 py-3 border border-zinc-600 bg-transparent text-white placeholder-zinc-400 rounded-lg focus:outline-none focus:ring-2 focus:ring-teal-400 transition" />
        </div>
        <div class="text-right">
          <input type="submit" value="Update User"
            class="px-6 py-3 bg-green-500 text-white font-semibold rounded-lg hover:bg-green-600 transition cursor-pointer" />
        </div>
      </form>
    </div>
  </div>
</body>

</html>